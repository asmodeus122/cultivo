--[[
    📜 Script: Auto Cultivation & Auto Rebirth
    📌 Jogo: Old Eternal Cultivation (Roblox)
    🛠️ Funções: 
        - Cultivar automaticamente
        - Renascer automaticamente
        - Tentativa de multiplicar RP ganhos
    ⚠️ Requer executor compatível (Synapse X, Delta, Fluxus)
    ❗ Substitua os nomes dos RemoteEvents se necessário
--]]

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Remote = ReplicatedStorage:WaitForChild("RemoteEvent") -- 🔁 Substitua por nome real se necessário

local delayTime = 2 -- Delay entre ações

-- Auto Cultivation + Rebirth
while true do
    task.wait(delayTime)

    -- Cultivate
    pcall(function()
        Remote:FireServer("Cultivate") -- 🧘‍♂️ Substitua por nome correto se diferente
    end)

    -- Rebirth
    pcall(function()
        Remote:FireServer("Rebirth") -- 🔁 Substitua por nome correto se diferente
    end)

    -- Tentativa de ganhar RP multiplicado
    pcall(function()
        Remote:FireServer("GainRP", 9999) -- 💥 Tentativa (pode não funcionar)
    end)
end
